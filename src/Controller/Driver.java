package Controller;

//Driver for project
public class Driver {

	public static void main(String[] args) throws Exception {
		MemoryController controller = new MemoryController();
		controller.interact(); //creates MemoryController and tells it to build the GUI
	}
}

